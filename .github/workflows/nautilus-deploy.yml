# Generated by nautilus-cli init-ci
# Automates: build EIF → SCP to EC2 → run nitro-cli
name: Deploy Enclave

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # ── Build the Nitro Enclave Image ────────────────────────────────
      - name: Build Docker Image (linux/amd64)
        run: |
          mkdir -p out
          docker build \
            --tag local/enclaveos:${{ github.sha }} \
            --platform linux/amd64 \
            --progress=plain \
            -f Containerfile \
            .

      - name: Build Nitro Enclave Image (.eif)
        run: |
          # nitro-cli is available on the ubuntu-latest runner via:
          # sudo amazon-linux-extras install aws-nitro-enclaves-cli  (Amazon Linux)
          # For GitHub runners, install the binary directly:
          sudo apt-get install -y aws-nitro-enclaves-cli || true
          nitro-cli build-enclave \
            --docker-uri local/enclaveos:${{ github.sha }} \
            --output-file out/nautilus.eif \
            | tee out/build.log
          # Extract PCRs to a sidecar JSON
          grep '{' out/build.log | tail -1 > out/nautilus.pcrs.json

      # ── Copy artifacts to EC2 ────────────────────────────────────────
      - name: Copy EIF to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.TEE_EC2_HOST }}
          username: ${{ secrets.TEE_EC2_USER }}
          key: ${{ secrets.TEE_EC2_SSH_KEY }}
          source: "out/nautilus.eif,out/nautilus.pcrs.json"
          target: "~/nautilus/"
          overwrite: true

      # ── Run the enclave on EC2 ───────────────────────────────────────
      - name: Deploy Enclave on EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.TEE_EC2_HOST }}
          username: ${{ secrets.TEE_EC2_USER }}
          key: ${{ secrets.TEE_EC2_SSH_KEY }}
          script: |
            set -e

            echo "=== Terminating existing enclaves ==="
            sudo nitro-cli terminate-enclave --all || true

            echo "=== Starting new enclave ==="
            sudo nitro-cli run-enclave \
              --cpu-count 2 \
              --memory 4096 \
              --eif-path ~/nautilus/nautilus.eif

            echo "=== Running enclaves ==="
            sudo nitro-cli describe-enclaves

            echo ""
            echo "✔ Deployment complete."
            echo "  Run: nautilus attest --host <host> to verify attestation."
